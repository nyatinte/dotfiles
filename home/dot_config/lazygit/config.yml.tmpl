git:
  pagers:
    - colorArg: always
      pager: delta --dark --paging=never --side-by-side --line-numbers --syntax-theme=Dracula --width=variable --navigate
{{- if env "GH_HOST" }}
services:
  {{ env "GH_HOST" | printf "%q" }}: {{ env "GH_HOST" | printf "github:%s" | printf "%q" }}
{{- end }}
