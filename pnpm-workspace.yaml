allowBuilds:
  lefthook: true

# pnpm itself is managed by mise (see home/dot_config/mise/config.toml) and its
# version is pinned via package.json's packageManager field, so don't
# record/enforce it in the lockfile.
pmOnFail: ignore

saveExact: true
