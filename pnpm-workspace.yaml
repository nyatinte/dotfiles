allowBuilds:
  lefthook: true

# pnpm is installed explicitly (standalone native binary) and its version is
# managed outside the project, so don't record/enforce it in the lockfile.
pmOnFail: ignore

saveExact: true
